export type HeroBadgeTier = {
  id: string;
  label: string;
  imageSrc: string;
  condition: string;
  description: string;
};

export type CollectibleIcon = {
  id: string;
  label: string;
  imageSrc: string;
  alt: string;
};
